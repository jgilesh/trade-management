# Trade Management — Options 101 Module 6

*From fill to exit.*

Standalone education module for the demystified Options 101 Hub (https://options101-hub.vercel.app/).

- `index.html` — the whole module. Single file, no build step.
- Deploy: Vercel, Framework Preset "Other", build and output fields blank.

## Editing links

Resource URLs live in the `LINK` object near the top of `index.html`.
Two are placeholders and render as "soon" pills until filled in:

- `LINK.journal` — Dominique's Trade Journal
- `LINK.risk` — Risk Management module
